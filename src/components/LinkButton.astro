---
const { class: className, title, href } = Astro.props
---
<div 
    class={`${className} 
    flex flex-row justify-center items-center p-1 w-fit 
    border rounded-md 
    bg-slate-600
    hover:bg-slate-900 hover:translate-y-0.5`
    }>
    <a class="flex flex-row justify-center items-center" href={href}>
        <slot />
        <p class="text-xs font-medium">{title}</p>
    </a>
</div>